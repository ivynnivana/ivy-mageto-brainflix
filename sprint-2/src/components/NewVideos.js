import React, { Component } from "react";
import Videos from "../components/Videos";

// function NewVideos({videoInfo, id}) {

//     let asideVideos = videoInfo.map((video, index) => {
//         if(video.id)
//     })
// }
